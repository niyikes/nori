{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "note-capability",
  "description": "Capability for dynamically created sticky note windows",
  "windows": ["note-*"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}